(jbuild_version 1)

(executable
 ((name test_ppx)
  (libraries (ppx pendulum js_of_ocaml react))
  (preprocess (pps (ppx js_of_ocaml.ppx)))
))

(alias
 ((name   runtest)
  (action (run ./test_ppx.exe))))

; (alias
;  ((name runtest)
;   (package lwt_react)
;   (action (run ${exe:main.exe}))))
